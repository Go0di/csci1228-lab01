PREDICTIONS

Trace 1

    3 // 7 & 2 are both integers, so java performs integer division, discarding the reminder

    3.5 // Explicitly casting (b) to  double promotes the division to floating-point

    3.0 // Integers division happens first, then 3 is cast to a double 3.0

    0.5 // integer division happens 1/2 = 0, then adds  0 + 0.5 = 0.5

    0.3 *** For this I had assumed, it will just do floating division ***


Trace 2

    10 // for loop goes through i = 1, 2, 3, 4, 5 and sums them up.

    -1 // runs n = n - 2 only while n > 0, when n returns < 0 the program stops there.


Trace 3

    done // the outer "else" statement attaches to the nearest preceding "if", regardless of indentation, 
            the entire inner block code is skipped completely because the outer condition "if (score > 90)"
            evaluates to "false" because score is 45. Execution jumps directly to the last line which prints "done".


RESULTS 

Trace 1

    3
    3.5
    3.0
    0.5
    0.30000000000000004

Trace 2

    10
    -1

Trace 3

    done


